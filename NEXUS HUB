if not bit32 then
    bit32 = {
        band  = function(a, b) return a & b end,
        bor   = function(a, b) return a | b end,
        bxor  = function(a, b) return a ~ b end,
        rshift = function(a, b) return a >> b end,
        lshift = function(a, b) return a << b end,
        bnot  = function(a)    return ~a end,
    }
end
if not getfenv then
    getfenv = function() return _ENV or {} end
end
if not string.unpack then
    string.unpack = function() return 0, 1 end
end
local Players         = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService    = game:GetService("TweenService")
local LocalPlayer     = Players.LocalPlayer
local Speed    = 500
local MinSpeed = 1
local MaxSpeed = 950
local Enabled  = false
local Gui = Instance.new("ScreenGui")
Gui.Name           = "NexusHubSpeed"
Gui.ResetOnSpawn   = false
Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
Gui.Parent         = LocalPlayer:WaitForChild("PlayerGui")
local Main = Instance.new("Frame")
Main.Name               = "Main"
Main.Size               = UDim2.fromOffset(310, 200)
Main.Position           = UDim2.new(0.5, -155, 0.5, -100)
Main.BackgroundColor3   = Color3.fromRGB(12, 12, 22)
Main.BorderSizePixel    = 0
Main.ClipsDescendants   = true
Main.Parent             = Gui
local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 16)
MainCorner.Parent       = Main
local BG = Instance.new("UIGradient")
BG.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(18, 12, 35)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 10, 22)),
})
BG.Rotation = 135
BG.Parent   = Main
local Stroke = Instance.new("UIStroke")
Stroke.Color           = Color3.fromRGB(120, 80, 255)
Stroke.Thickness       = 1.5
Stroke.Transparency    = 0.15
Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
Stroke.Parent          = Main
local Header = Instance.new("Frame")
Header.Name                = "Header"
Header.Size                = UDim2.new(1, 0, 0, 46)
Header.BackgroundTransparency = 1
Header.BorderSizePixel     = 0
Header.Parent              = Main
local HGrad = Instance.new("UIGradient")
HGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0,   Color3.fromRGB(90,  40, 200)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(120, 60, 240)),
    ColorSequenceKeypoint.new(1,   Color3.fromRGB(60, 100, 255)),
})
HGrad.Rotation = 90
HGrad.Parent   = Header
local LogoBox = Instance.new("Frame")
LogoBox.Size                = UDim2.fromOffset(28, 28)
LogoBox.Position            = UDim2.fromOffset(12, 9)
LogoBox.BackgroundColor3    = Color3.fromRGB(255, 255, 255)
LogoBox.BackgroundTransparency = 0.75
LogoBox.BorderSizePixel     = 0
LogoBox.Parent              = Header
local LogoCorner = Instance.new("UICorner")
LogoCorner.CornerRadius = UDim.new(0, 7)
LogoCorner.Parent       = LogoBox
local LogoText = Instance.new("TextLabel")
LogoText.Size               = UDim2.new(1, 0, 1, 0)
LogoText.BackgroundTransparency = 1
LogoText.Text               = "N"
LogoText.TextColor3         = Color3.fromRGB(255, 255, 255)
LogoText.TextSize           = 16
LogoText.Font               = Enum.Font.GothamBlack
LogoText.TextXAlignment     = Enum.TextXAlignment.Center
LogoText.Parent             = LogoBox
local Title = Instance.new("TextLabel")
Title.Size               = UDim2.new(1, -100, 0, 46)
Title.Position           = UDim2.fromOffset(48, 0)
Title.BackgroundTransparency = 1
Title.Text               = "NEXUS HUB"
Title.TextColor3         = Color3.fromRGB(255, 255, 255)
Title.TextSize           = 16
Title.Font               = Enum.Font.GothamBlack
Title.TextXAlignment     = Enum.TextXAlignment.Left
Title.TextYAlignment     = Enum.TextYAlignment.Center
Title.Parent             = Header
local Badge = Instance.new("TextLabel")
Badge.Size               = UDim2.fromOffset(52, 15)
Badge.Position           = UDim2.new(0, 48, 0, 29)
Badge.BackgroundColor3   = Color3.fromRGB(255, 255, 255)
Badge.BackgroundTransparency = 0.8
Badge.Text               = "SPEED"
Badge.TextColor3         = Color3.fromRGB(200, 180, 255)
Badge.TextSize           = 9
Badge.Font               = Enum.Font.GothamBold
Badge.TextXAlignment     = Enum.TextXAlignment.Center
Badge.Parent             = Header
local BadgeCorner = Instance.new("UICorner")
BadgeCorner.CornerRadius = UDim.new(0, 4)
BadgeCorner.Parent       = Badge
local Close = Instance.new("TextButton")
Close.Size               = UDim2.fromOffset(28, 28)
Close.Position           = UDim2.new(1, -40, 0, 9)
Close.BackgroundColor3   = Color3.fromRGB(255, 80, 100)
Close.BackgroundTransparency = 0.3
Close.Text               = "x"
Close.TextColor3         = Color3.fromRGB(255, 255, 255)
Close.TextSize           = 15
Close.Font               = Enum.Font.GothamBlack
Close.AutoButtonColor    = false
Close.Parent             = Header
local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent       = Close
local MinBtn = Instance.new("TextButton")
MinBtn.Size               = UDim2.fromOffset(28, 28)
MinBtn.Position           = UDim2.new(1, -74, 0, 9)
MinBtn.BackgroundColor3   = Color3.fromRGB(255, 190, 50)
MinBtn.BackgroundTransparency = 0.3
MinBtn.Text               = "-"
MinBtn.TextColor3         = Color3.fromRGB(255, 255, 255)
MinBtn.TextSize           = 18
MinBtn.Font               = Enum.Font.GothamBlack
MinBtn.AutoButtonColor    = false
MinBtn.Parent             = Header
local MinCorner = Instance.new("UICorner")
MinCorner.CornerRadius = UDim.new(0, 8)
MinCorner.Parent       = MinBtn
local Sep = Instance.new("Frame")
Sep.Size             = UDim2.new(1, 0, 0, 1)
Sep.Position         = UDim2.fromOffset(0, 46)
Sep.BackgroundColor3 = Color3.fromRGB(120, 80, 255)
Sep.BackgroundTransparency = 0.6
Sep.BorderSizePixel  = 0
Sep.Parent           = Main
local Content = Instance.new("Frame")
Content.Size                = UDim2.new(1, 0, 1, -47)
Content.Position            = UDim2.fromOffset(0, 47)
Content.BackgroundTransparency = 1
Content.BorderSizePixel     = 0
Content.Parent              = Main
local SpeedRow = Instance.new("Frame")
SpeedRow.Size               = UDim2.new(1, -40, 0, 28)
SpeedRow.Position           = UDim2.fromOffset(20, 12)
SpeedRow.BackgroundTransparency = 1
SpeedRow.Parent             = Content
local SpeedLabel = Instance.new("TextLabel")
SpeedLabel.Size             = UDim2.new(1, -65, 1, 0)
SpeedLabel.BackgroundTransparency = 1
SpeedLabel.Text             = "WalkSpeed"
SpeedLabel.TextColor3       = Color3.fromRGB(200, 185, 255)
SpeedLabel.TextSize         = 13
SpeedLabel.Font             = Enum.Font.Gotham
SpeedLabel.TextXAlignment   = Enum.TextXAlignment.Left
SpeedLabel.Parent           = SpeedRow
local SpeedNum = Instance.new("TextLabel")
SpeedNum.Size               = UDim2.fromOffset(65, 28)
SpeedNum.Position           = UDim2.new(1, -65, 0, 0)
SpeedNum.BackgroundTransparency = 1
SpeedNum.TextColor3         = Color3.fromRGB(160, 130, 255)
SpeedNum.TextSize           = 20
SpeedNum.Font               = Enum.Font.GothamBlack
SpeedNum.TextXAlignment     = Enum.TextXAlignment.Right
SpeedNum.Parent             = SpeedRow
local SliderTrack = Instance.new("Frame")
SliderTrack.Name            = "Slider"
SliderTrack.Size            = UDim2.new(1, -40, 0, 6)
SliderTrack.Position        = UDim2.fromOffset(20, 54)
SliderTrack.BackgroundColor3 = Color3.fromRGB(40, 35, 65)
SliderTrack.BorderSizePixel = 0
SliderTrack.Parent          = Content
local SliderCorner = Instance.new("UICorner")
SliderCorner.CornerRadius = UDim.new(1, 0)
SliderCorner.Parent       = SliderTrack
local Fill = Instance.new("Frame")
Fill.BackgroundColor3 = Color3.fromRGB(120, 80, 255)
Fill.BorderSizePixel  = 0
Fill.Parent           = SliderTrack
local FillCorner = Instance.new("UICorner")
FillCorner.CornerRadius = UDim.new(1, 0)
FillCorner.Parent       = Fill
local FillGrad = Instance.new("UIGradient")
FillGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(100, 60, 220)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(80, 160, 255)),
})
FillGrad.Parent = Fill
local Knob = Instance.new("TextButton")
Knob.Size             = UDim2.fromOffset(20, 20)
Knob.AnchorPoint      = Vector2.new(0.5, 0.5)
Knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Knob.Text             = ""
Knob.AutoButtonColor  = false
Knob.ZIndex           = 5
Knob.Parent           = SliderTrack
local KnobCorner = Instance.new("UICorner")
KnobCorner.CornerRadius = UDim.new(1, 0)
KnobCorner.Parent       = Knob
local KnobDot = Instance.new("Frame")
KnobDot.Size             = UDim2.fromOffset(8, 8)
KnobDot.AnchorPoint      = Vector2.new(0.5, 0.5)
KnobDot.Position         = UDim2.new(0.5, 0, 0.5, 0)
KnobDot.BackgroundColor3 = Color3.fromRGB(120, 80, 255)
KnobDot.BorderSizePixel  = 0
KnobDot.ZIndex           = 6
KnobDot.Parent           = Knob
local KnobDotCorner = Instance.new("UICorner")
KnobDotCorner.CornerRadius = UDim.new(1, 0)
KnobDotCorner.Parent       = KnobDot
local LabelMin = Instance.new("TextLabel")
LabelMin.Size               = UDim2.fromOffset(30, 14)
LabelMin.Position           = UDim2.fromOffset(20, 64)
LabelMin.BackgroundTransparency = 1
LabelMin.Text               = tostring(MinSpeed)
LabelMin.TextColor3         = Color3.fromRGB(100, 90, 140)
LabelMin.TextSize           = 10
LabelMin.Font               = Enum.Font.Gotham
LabelMin.TextXAlignment     = Enum.TextXAlignment.Left
LabelMin.Parent             = Content
local LabelMax = Instance.new("TextLabel")
LabelMax.Size               = UDim2.fromOffset(40, 14)
LabelMax.Position           = UDim2.new(1, -60, 0, 64)
LabelMax.BackgroundTransparency = 1
LabelMax.Text               = tostring(MaxSpeed)
LabelMax.TextColor3         = Color3.fromRGB(100, 90, 140)
LabelMax.TextSize           = 10
LabelMax.Font               = Enum.Font.Gotham
LabelMax.TextXAlignment     = Enum.TextXAlignment.Right
LabelMax.Parent             = Content
local UseButton = Instance.new("TextButton")
UseButton.Size             = UDim2.new(1, -40, 0, 42)
UseButton.Position         = UDim2.fromOffset(20, 90)
UseButton.BackgroundColor3 = Color3.fromRGB(60, 45, 110)
UseButton.Text             = ""
UseButton.AutoButtonColor  = false
UseButton.ZIndex           = 2
UseButton.Parent           = Content
local UseCorner = Instance.new("UICorner")
UseCorner.CornerRadius = UDim.new(0, 12)
UseCorner.Parent       = UseButton
local BtnGrad = Instance.new("UIGradient")
BtnGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(70, 50, 130)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(50, 35, 95)),
})
BtnGrad.Rotation = 90
BtnGrad.Parent   = UseButton
local BtnStroke = Instance.new("UIStroke")
BtnStroke.Color       = Color3.fromRGB(120, 80, 255)
BtnStroke.Thickness   = 1
BtnStroke.Transparency = 0.5
BtnStroke.Parent      = UseButton
local BtnIcon = Instance.new("TextLabel")
BtnIcon.Size             = UDim2.fromOffset(30, 42)
BtnIcon.Position         = UDim2.fromOffset(14, 0)
BtnIcon.BackgroundTransparency = 1
BtnIcon.Text             = ">"
BtnIcon.TextColor3       = Color3.fromRGB(210, 195, 255)
BtnIcon.TextSize         = 18
BtnIcon.Font             = Enum.Font.GothamBlack
BtnIcon.ZIndex           = 3
BtnIcon.Parent           = UseButton
local BtnLabel = Instance.new("TextLabel")
BtnLabel.Size             = UDim2.new(1, -50, 1, 0)
BtnLabel.Position         = UDim2.fromOffset(42, 0)
BtnLabel.BackgroundTransparency = 1
BtnLabel.Text             = "ACTIVATE SPEED"
BtnLabel.TextColor3       = Color3.fromRGB(210, 195, 255)
BtnLabel.TextSize         = 13
BtnLabel.Font             = Enum.Font.GothamBold
BtnLabel.TextXAlignment   = Enum.TextXAlignment.Left
BtnLabel.ZIndex           = 3
BtnLabel.Parent           = UseButton
local BtnStatus = Instance.new("TextLabel")
BtnStatus.Size             = UDim2.fromOffset(40, 20)
BtnStatus.Position         = UDim2.new(1, -50, 0.5, -10)
BtnStatus.BackgroundColor3 = Color3.fromRGB(80, 60, 160)
BtnStatus.Text             = "OFF"
BtnStatus.TextColor3       = Color3.fromRGB(180, 160, 255)
BtnStatus.TextSize         = 10
BtnStatus.Font             = Enum.Font.GothamBold
BtnStatus.ZIndex           = 3
BtnStatus.Parent           = UseButton
local StatusCorner = Instance.new("UICorner")
StatusCorner.CornerRadius = UDim.new(0, 5)
StatusCorner.Parent       = BtnStatus
local OpenButton = Instance.new("ImageButton")
OpenButton.Size             = UDim2.fromOffset(52, 52)
OpenButton.Position         = UDim2.fromOffset(20, 200)
OpenButton.BackgroundColor3 = Color3.fromRGB(80, 45, 200)
OpenButton.Image            = "https://s.imgz.io/2026/08/28/c8db47f8e7d87ad6c69184d13b48233f476d187af036c133.jpg"
OpenButton.ScaleType        = Enum.ScaleType.Crop
OpenButton.Visible          = false
OpenButton.Parent           = Gui
local OpenCorner = Instance.new("UICorner")
OpenCorner.CornerRadius = UDim.new(1, 0)
OpenCorner.Parent       = OpenButton
local OpenStroke = Instance.new("UIStroke")
OpenStroke.Color       = Color3.fromRGB(120, 80, 255)
OpenStroke.Thickness   = 2
OpenStroke.Transparency = 0.2
OpenStroke.Parent      = OpenButton
local function ApplySpeed()
    local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local Humanoid  = Character:FindFirstChildOfClass("Humanoid")
    if Humanoid then
        Humanoid:Destroy()
        task.wait(0.05)
    end
    local NewHumanoid = Instance.new("Humanoid")
    NewHumanoid.Parent    = Character
    NewHumanoid.WalkSpeed = Speed
end
local function ChangeCurrentSpeed()
    local Character = LocalPlayer.Character
    if not Character then return end
    local Humanoid = Character:FindFirstChildOfClass("Humanoid")
    if Humanoid then Humanoid.WalkSpeed = Speed end
end
local Sliding = false
local function UpdateSlider(X)
    local Start   = SliderTrack.AbsolutePosition.X
    local Width   = SliderTrack.AbsoluteSize.X
    local Percent = math.clamp((X - Start) / Width, 0, 1)
    Speed         = math.floor(MinSpeed + ((MaxSpeed - MinSpeed) * Percent))
    SpeedNum.Text = tostring(Speed)
    Fill.Size     = UDim2.new(Percent, 0, 1, 0)
    Knob.Position = UDim2.new(Percent, 0, 0.5, 0)
    if Enabled then ChangeCurrentSpeed() end
end
SliderTrack.InputBegan:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.MouseButton1 then
        Sliding = true
        UpdateSlider(Input.Position.X)
    end
end)
Knob.InputBegan:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.MouseButton1 then
        Sliding = true
    end
end)
UserInputService.InputChanged:Connect(function(Input)
    if Sliding and Input.UserInputType == Enum.UserInputType.MouseMovement then
        UpdateSlider(Input.Position.X)
    end
end)
UserInputService.InputEnded:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.MouseButton1 then
        Sliding  = false
        Dragging = false
    end
end)
UseButton.MouseButton1Click:Connect(function()
    Enabled = not Enabled
    if Enabled then
        BtnLabel.Text            = "SPEED ACTIVE"
        BtnLabel.TextColor3      = Color3.fromRGB(160, 255, 180)
        BtnIcon.Text             = "!"
        BtnIcon.TextColor3       = Color3.fromRGB(120, 255, 150)
        BtnStatus.Text           = "ON"
        BtnStatus.TextColor3     = Color3.fromRGB(120, 255, 150)
        BtnStatus.BackgroundColor3 = Color3.fromRGB(30, 90, 50)
        BtnGrad.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 100, 55)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 70, 40)),
        })
        BtnStroke.Color = Color3.fromRGB(80, 220, 120)
        ApplySpeed()
    else
        BtnLabel.Text            = "ACTIVATE SPEED"
        BtnLabel.TextColor3      = Color3.fromRGB(210, 195, 255)
        BtnIcon.Text             = ">"
        BtnIcon.TextColor3       = Color3.fromRGB(210, 195, 255)
        BtnStatus.Text           = "OFF"
        BtnStatus.TextColor3     = Color3.fromRGB(180, 160, 255)
        BtnStatus.BackgroundColor3 = Color3.fromRGB(80, 60, 160)
        BtnGrad.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(70, 50, 130)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(50, 35, 95)),
        })
        BtnStroke.Color = Color3.fromRGB(120, 80, 255)
        local Character = LocalPlayer.Character
        local Humanoid  = Character and Character:FindFirstChildOfClass("Humanoid")
        if Humanoid then Humanoid.WalkSpeed = 16 end
    end
end)
LocalPlayer.CharacterAdded:Connect(function()
    if Enabled then
        task.wait(0.2)
        ApplySpeed()
    end
end)
local Dragging     = false
local DragStart    = nil
local StartPos     = nil
Header.InputBegan:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.MouseButton1 then
        Dragging  = true
        DragStart = Input.Position
        StartPos  = Main.Position
    end
end)
UserInputService.InputChanged:Connect(function(Input)
    if Dragging and Input.UserInputType == Enum.UserInputType.MouseMovement then
        local Delta = Input.Position - DragStart
        Main.Position = UDim2.new(
            StartPos.X.Scale, StartPos.X.Offset + Delta.X,
            StartPos.Y.Scale, StartPos.Y.Offset + Delta.Y
        )
    end
end)
Close.MouseButton1Click:Connect(function()
    TweenService:Create(Main, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {
        Size = UDim2.fromOffset(310, 0),
        BackgroundTransparency = 1,
    }):Play()
    task.wait(0.18)
    Main.Visible              = false
    Main.Size                 = UDim2.fromOffset(310, 200)
    Main.BackgroundTransparency = 0
    OpenButton.Visible        = true
end)
OpenButton.MouseButton1Click:Connect(function()
    Main.Visible       = true
    OpenButton.Visible = false
end)
local minimized = false
MinBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    Content.Visible = not minimized
    Sep.Visible     = not minimized
    if minimized then
        Main.Size    = UDim2.fromOffset(310, 46)
        MinBtn.Text  = "+"
    else
        Main.Size    = UDim2.fromOffset(310, 200)
        MinBtn.Text  = "-"
    end
end)
local InitPercent = (Speed - MinSpeed) / (MaxSpeed - MinSpeed)
SpeedNum.Text = tostring(Speed)
Fill.Size     = UDim2.new(InitPercent, 0, 1, 0)
Knob.Position = UDim2.new(InitPercent, 0, 0.5, 0)
